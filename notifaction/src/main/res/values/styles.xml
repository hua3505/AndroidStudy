<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="Notifaction.Text.Title" parent="@android:style/TextAppearance.StatusBar.EventContent.Title" />
    <style name="Notifaction.Text.Content" parent="@android:style/TextAppearance.StatusBar.EventContent.Info" />

</resources>
